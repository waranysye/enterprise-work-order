[build]
builder = "nixpacks"
buildCommand = "npm ci && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && npx ts-node --project tsconfig.seed.json prisma/seed-safe.ts && node server.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
